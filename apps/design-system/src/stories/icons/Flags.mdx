import { Meta } from '@storybook/blocks';
import * as icons from '@shared/icons-flags'
import { IconGallery } from '../../components'

<Meta
  title="Icons/Flags"
/>

<IconGallery title={'Flags icons'} icons={icons} />
